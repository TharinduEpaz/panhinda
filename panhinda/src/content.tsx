export const fetchContent = async (id: string) => {
  // Dummy implementation
  return null;
};

export const saveContent = async (id: string, content: any) => {
  // Dummy implementation
  return true;
};
